name: rota_das_oficinas_test
description: Projeto para o teste da Rota das Oficinas

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  cupertino_icons: ^1.0.2
  formz: ^0.6.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  collection: ^1.17.1
  go_router: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true